This repository uses zig to build. to build this, run 

```
zig build
```

the executable appears in the `zig-out` folder.

This is a desktop application, with most of the frontend existing in `src/assets`
