name: Release

on:
  push:
    tags: ['v*.*']
  pull_request:
  workflow_dispatch:

jobs:
  build:
    name: Build (${{ matrix.target }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          # Linux — needs GTK3 + WebKitGTK system libraries
          - os: ubuntu-22.04
            target: linux-x86_64
            zig_target: x86_64-linux-gnu
            pkg: tar

          # macOS — needs WebKit.framework (ships with macOS SDK)
          - os: macos-latest
            target: macos-aarch64
            zig_target: aarch64-macos
            pkg: dmg
          - os: macos-13
            target: macos-x86_64
            zig_target: x86_64-macos
            pkg: dmg

          # Windows — needs WebView2 headers (bundled by the webview dep)
          - os: windows-latest
            target: windows-x86_64
            zig_target: x86_64-windows
            pkg: zip

    steps:
      - uses: actions/checkout@v4
      - uses: mlugg/setup-zig@v2
        with:
          version: "0.15.2"

      # Linux: install GTK3 and WebKitGTK dev headers for webview
      - name: Install Linux dependencies
        if: runner.os == 'Linux'
        run: |
          sudo apt-get update
          sudo apt-get install -y libgtk-3-dev libwebkit2gtk-4.1-dev

      - name: Build
        run: zig build -Doptimize=ReleaseSmall -Dtarget=${{ matrix.zig_target }} -Dversion=${{ github.ref_name }}

      # --- Platform-specific packaging ---

      - name: Package tarball (Linux)
        if: matrix.pkg == 'tar'
        run: |
          chmod +x zig-out/bin/krustyklaw
          tar -czf krustyklaw-${{ matrix.target }}.tar.gz -C zig-out/bin krustyklaw

      - name: Package DMG (macOS)
        if: matrix.pkg == 'dmg'
        run: |
          mkdir -p "KrustyKlaw.app/Contents/MacOS"
          cp zig-out/bin/krustyklaw "KrustyKlaw.app/Contents/MacOS/"
          chmod +x "KrustyKlaw.app/Contents/MacOS/krustyklaw"
          cat > "KrustyKlaw.app/Contents/Info.plist" << 'EOF'
          <?xml version="1.0" encoding="UTF-8"?>
          <!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
          <plist version="1.0">
          <dict>
            <key>CFBundleExecutable</key>
            <string>krustyklaw</string>
            <key>CFBundleName</key>
            <string>KrustyKlaw</string>
            <key>CFBundleIdentifier</key>
            <string>io.nullclaw.krustyklaw</string>
            <key>CFBundleVersion</key>
            <string>${{ github.ref_name }}</string>
            <key>CFBundlePackageType</key>
            <string>APPL</string>
          </dict>
          </plist>
          EOF
          hdiutil create -volname "KrustyKlaw" -srcfolder "KrustyKlaw.app" -ov -format UDZO krustyklaw-${{ matrix.target }}.dmg

      - name: Package zip (Windows)
        if: matrix.pkg == 'zip'
        shell: pwsh
        run: Compress-Archive -Path zig-out/bin/krustyklaw.exe -DestinationPath krustyklaw-${{ matrix.target }}.zip

      - uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.target }}
          path: |
            krustyklaw-*.tar.gz
            krustyklaw-*.dmg
            krustyklaw-*.zip

  release:
    needs: build
    if: startsWith(github.ref, 'refs/tags/v')
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/download-artifact@v4
        with:
          path: dist
          merge-multiple: true

      - name: Generate checksums
        run: |
          cd dist
          sha256sum * > checksums-sha256.txt

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          files: dist/*
          generate_release_notes: true
